# RANDAO Oracle

A trustless on-chain RANDAO oracle supporting all post-merge Ethereum blocks. The contract uses Relic Protocol to cryptographically verify the block's header and extracts the RANDAO value.

